### .tigrc

```
# main viewの左端にコミットID(5文字)を表示する
set main-view = id:width=5 date author commit-title:graph=yes,refs=yes
# 画面を垂直方向に分割する
set -split = yes
# 水平分割したウィンドウの下画面サイズを % で指定（行数指定も可）
set split-view-height = 80%
```
